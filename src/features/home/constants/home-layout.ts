/** Layout tokens for responsive Home cards (narrow phones). */
export const HOME_LAYOUT = {
  statTileWidth: 'w-[48%]',
  statTileMin: 'min-w-[140px]',
  rowWrap: 'flex-row flex-wrap items-start gap-2',
  rowWrapCenter: 'flex-row flex-wrap items-center gap-2',
  shrinkBlock: 'min-w-0 shrink',
  growBlock: 'min-w-0 flex-1 grow',
} as const
