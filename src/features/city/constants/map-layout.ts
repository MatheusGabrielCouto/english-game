export const CITY_MAP_HEIGHT = 520;

/** Canvas maior que o viewport — permite arrastar o mapa (estilo Google Maps). */
export const CITY_MAP_CANVAS_SCALE = 1.32;

/** Largura aproximada do pin (ícone + label). */
export const CITY_MAP_PIN_CENTER_OFFSET_X = 36;

export const CITY_MAP_PIN_CENTER_OFFSET_Y = 32;
