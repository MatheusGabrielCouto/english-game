/** Press Start 2P — display RPG para títulos, níveis e valores de jogo. */
export const fontFamilies = {
  display: 'PressStart2P_400Regular',
  body: undefined,
} as const

export type FontFamilyKey = keyof typeof fontFamilies
